mixin search-form(keyword)
   form(form-search="")
      .input-group 
         input(
            class="form-control"
            type="text"
            placeholder="Nhập từ khoá"
            name="inputKeyword"
            value=keyword
         )
         .input-group-append 
            button(class="btn btn-success" type="submit") Tìm