mixin sub-menu(listData)
   ul
      each item in listData
         li
            a(href=`/products/${item.slug}`) #{item.title}
            
            - if(item.children)
               +sub-menu(item.children)